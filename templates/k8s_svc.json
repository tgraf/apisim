{
    "kind":"Service",
    "apiVersion":"v1",
    "metadata":{
        "name":"{{.Name}}",
        "labels":{
            "app":"{{.Name}}"
        }
    },
    "spec":{
        "type": "NodePort",
        "ports": [
                {{.Ports}}
        ],
        "selector":{
            "app":"{{.Name}}"
        }
    }
}
